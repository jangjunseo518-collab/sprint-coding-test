class Solution {
    public int solution(String s) {
        int answer = Integer.parseInt(s);
        //Integer.parseInt()는 문자열을 숫자로 바꿔주는 java내장 메서드
        // 문자열에 숫자가 들어있을때는 정상 작동 문자열에 문자가 들어있으면 오류.
        return answer;
    }
}